<UserControl x:Class="VodovozSPB.Helpers.CustomMenuItem"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Margin="5" Content="{Binding Header1}" Command="{Binding CommandAction}" CommandParameter="{Binding CommandParameter1}"/>
        <Button Grid.Row="1" Margin="5" Content="{Binding Header2}" Command="{Binding CommandAction}" CommandParameter="{Binding CommandParameter2}"/>
    </Grid>
</UserControl>
